{
  "manifest_version": 3,
  "name": "Reddit Post JSON Ingest",
  "version": "0.1.0",
  "description": "Collects current Reddit post (title, body, comments, metadata) and sends JSON to local FastAPI endpoint.",
  "permissions": [],
  "content_scripts": [
    {
      "matches": ["*://*.reddit.com/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "host_permissions": ["*://*.reddit.com/*"]
}
